# Nginx Configuration for Multiple Sites
# /etc/nginx/sites-available/royal-nuts

# Royal Nuts (royalnuts.uz) - Port 3002
server {
    listen 80;
    server_name royalnuts.uz www.royalnuts.uz;

    location / {
        proxy_pass http://localhost:3002;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }
}

# Doctors IELTS (doctorsielts.uz) - Port 3001
server {
    listen 80;
    server_name doctorsielts.uz www.doctorsielts.uz;

    location / {
        proxy_pass http://localhost:3001;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }
}

# SSL uchun (Let's Encrypt keyin qo'shiladi)
# server {
#     listen 443 ssl;
#     server_name royalnuts.uz www.royalnuts.uz;
#     ssl_certificate /etc/letsencrypt/live/royalnuts.uz/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/royalnuts.uz/privkey.pem;
#     ...
# }

